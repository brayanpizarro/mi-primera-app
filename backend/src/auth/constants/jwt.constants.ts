/**
 * Constantes utilizadas para la autenticación JWT.
 *
 * @property secret - La clave secreta utilizada para firmar los tokens JWT.
 *   **Nota:** Este valor no debe usarse en entornos de producción.
 */
export const jwtConstanst = {
    secret: "Esta palabra no se debe usar en producción",
};